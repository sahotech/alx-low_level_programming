#include <main.h>
/**
 * main - Entry point
 * Description - print out alphabet in lowercase
 * return - 0 always
 */

int main(void)
{
	char c;
	for (c = 0; c <= z; c++);
	putchar(c);
	putchar(\n);
	return (0);
}
